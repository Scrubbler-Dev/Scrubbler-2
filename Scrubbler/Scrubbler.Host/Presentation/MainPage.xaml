<Page x:Class="Scrubbler.Host.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Scrubbler.Host.Presentation"
      xmlns:navigation="using:Scrubbler.Host.Presentation.Navigation"
      Background="{ThemeResource BackgroundBrush}">

    <Page.Resources>
        <DataTemplate x:Key="MenuItemTemplate" x:DataType="navigation:MenuNavigationItemViewModel">
            <NavigationViewItem SelectsOnInvoked="{x:Bind HasChildren, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}">
                <NavigationViewItem.Icon>
                    <IconSourceElement IconSource="{x:Bind Icon}" />
                </NavigationViewItem.Icon>

                <NavigationViewItem.Content>
                    <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                        <TextBlock Text="{x:Bind Title}" VerticalAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" Spacing="4" Visibility="{x:Bind HasErrors, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                            <SymbolIcon Symbol="Cancel" Foreground="Red"/>
                            <TextBlock Text="{x:Bind Errors, Mode=OneWay}" VerticalAlignment="Center" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="4" Visibility="{x:Bind HasWarnings, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                            <SymbolIcon Symbol="Important" Foreground="Yellow"/>
                            <TextBlock Text="{x:Bind Warnings, Mode=OneWay}" VerticalAlignment="Center" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="4" Visibility="{x:Bind HasInfos, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                            <SymbolIcon Symbol="Download" Foreground="LightBlue"/>
                            <TextBlock Text="{x:Bind Infos, Mode=OneWay}" VerticalAlignment="Center" />
                        </StackPanel>
                    </StackPanel>
                </NavigationViewItem.Content>

                <NavigationViewItem.MenuItemsSource>
                    <Binding Path="Children" />
                </NavigationViewItem.MenuItemsSource>
            </NavigationViewItem>
        </DataTemplate>

        <DataTemplate x:Key="PluginItemTemplate"
              x:DataType="navigation:PluginNavigationItemViewModel">
            <NavigationViewItem SelectsOnInvoked="True">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="8">
                    <ImageIcon Source="{x:Bind Icon}" Width="25" Height="25" VerticalAlignment="Center"/>
                    <TextBlock Text="{x:Bind Title}" VerticalAlignment="Center"/>
                </StackPanel>
            </NavigationViewItem>
        </DataTemplate>

        <navigation:NavigationTemplateSelector
            x:Key="NavigationSelector"
            MenuTemplate="{StaticResource MenuItemTemplate}"
            PluginTemplate="{StaticResource PluginItemTemplate}" />
    </Page.Resources>

    <NavigationView
        MenuItemsSource="{Binding Items}"
        MenuItemTemplateSelector="{StaticResource NavigationSelector}"
        SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
        IsBackButtonVisible="Collapsed"
        IsPaneToggleButtonVisible="True"
        IsSettingsVisible="False"
        PaneDisplayMode="Left"
        CompactPaneLength="80">

        <!-- ALL MAIN UI CONTENT GOES HERE -->
        <NavigationView.Content>
            <Grid>
                <!--<Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>-->

                <!-- Page or Plugin View -->
                <ContentPresenter Content="{Binding CurrentViewModel}"
                                  ContentTemplateSelector="{StaticResource ViewLocatorSelector}" />

                <!-- Global InfoBar -->
                <InfoBar x:Name="GlobalInfoBar"
                         IsOpen="False"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Top"
                         Margin="16"
                         IsClosable="True"/>
            </Grid>
        </NavigationView.Content>
    </NavigationView>
</Page>
